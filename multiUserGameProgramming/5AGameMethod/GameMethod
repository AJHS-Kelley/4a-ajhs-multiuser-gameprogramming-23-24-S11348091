// Amaure Cooper, Program Template, v0.0

using System;

namespace GameMethod
{

   class Program
   {

    static void CarModels()
    {
       // GameMethod  is the collection of memories stored within a program.
        
        /* Data
        -- within the data collected the avergaes of speed will increase
        -- Within the data collected the avergaes of speed will decrease
        --The motion of the cars are programed to show top speed
        */
        0 refernce 
       // Car Models
       {
         Console.writeLine("As you race, The speed and timing will be caculated.\n");
       }
        "For every car, The amount of time collected goes to The amount of distance."
      
      } else if (increase in distance){
            // speed increase.
        else if (modification = proformence)   
       }    // speed increase....determining system motion...
      }
    

       static void MotionCapture()
       {
       // Motion Capture

       //Motion:
       Console.writeLine(),
       Console.writeLine(int design, int picture);
       Void Output();
       motion capture- ( design image & Picture graphic);

       // Graphic:
       int D_Design;
       int P_Graphic;
       // Creating a image desgin to show the moving motion.
      }


     staticvoid Speed()
     {
      // Speed 
      int main()
     {
      Hello();
      // Change to Console.WriteLine() no print()
      Console.WriteLine("Hello,miles per hour messaged by timing!\n.");
     }
       Console.write(void)
     {
      Console.WriteLine("Hello, speed function generated by counting!\n.");
      Console.WriteLine(print speed count.)

    }
      
    }
      
    //Keep Main at the bottom
    static void Main(string[] args)
    {
      Console.WriteLine("Motion Collection: Gathering the speed of the car while driving\.n");
      Console.WriteLine("The account of distance repersent the cars motion\.n")
        if (Speed != distance) {

    } 
  }
  
}   
